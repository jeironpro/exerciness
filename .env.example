# Copia este archivo a .env para variables locales.
# Las variables .env* con secretos NUNCA deben llegar al remoto.
VITE_APP_NAME=exerciness
# Sin backend en v1: se deja vacía para futuras versiones.
VITE_API_BASE_URL=
